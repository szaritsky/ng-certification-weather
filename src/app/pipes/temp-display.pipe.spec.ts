import { TempDisplayPipe } from './temp-display.pipe';

describe('TempDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TempDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
